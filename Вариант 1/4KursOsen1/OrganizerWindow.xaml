<Window x:Class="_4KursOsen1.OrganizerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_4KursOsen1"
        mc:Ignorable="d"
        Title="OrganizerWindow"     
        MaxHeight="600" MaxWidth="1000"
    Height="450" Width="800"
    MinHeight="400" MinWidth="750"
    WindowStartupLocation="CenterScreen">
    <Window.Resources>
        
        <Style TargetType="Button">
            <Setter Property="Height" Value="80"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Окно организотора" HorizontalAlignment="Left" Grid.Column="1" FontSize="40"/>
        <StackPanel Grid.Row="1" Grid.Column="0">
            <Viewbox Width="150" Height="200">
                <Image x:Name="userImage" Width="150" Height="200" Stretch="UniformToFill"/>
            </Viewbox>
            <Button Width="150" Content="Мой профиль"  Click="toProfileButton_Click"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1">
            <TextBlock Text="Доброе утро" Name="timeTextBlock"/>
            <TextBlock Name ="welcomeTextBlock"/>
            <Button Content="Мероприятия" Click="EventsButton_Click"/>
            <Button Content="Участники" Click="ParticipantsButton_Click"/>
            <Button Content="Жюри/Модераторы" Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
